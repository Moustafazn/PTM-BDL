# ── IDE ──
.idea/
.vscode/
*.swp
*.swo

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg

# ── Virtual environments ──
.venv/
venv/
env/
.python-version

# ── Data (large files — not tracked) ──
data/

# ── OS artifacts ──
.DS_Store
Thumbs.db

# ── Dependency lock (reproducible via pyproject.toml) ──
uv.lock

# ── Research artifacts (not for publication) ──
research/

# ── Results (generated outputs — reproducible from code) ──
results/

# ── Internal docs (not for publication) ──
docs/ARCHITECTURE.md
docs/COMPREHENSIVE_EVALUATION_07_july.md

# ── Benchmarking (cloned external repos — large) ──
benchmarks/

# ── Manuscript (local only — not tracked until submission) ──
manuscript/

/docs/MANUSCRIPT_PLAN.md
/src/case_studies/common/generate_html_figures.py
/docs/TASK_ADD_NON_TKI_CASE_STUDIES.md
