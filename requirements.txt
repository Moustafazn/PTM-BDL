# ============================================================================
# requirements.txt — PTM-BDL tool
# ============================================================================
# Install:
#   pip install -r requirements.txt
#   pip install torch-geometric -f https://data.pyg.org/whl/torch-2.12.0+cpu.html
#
# Or use: pip install -e ".[dev]"  (recommended, uses pyproject.toml)
# ============================================================================

# ── Core Scientific Stack ──
pandas>=2.0
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
biopython>=1.84

# ── Deep Learning ──
torch>=2.1
transformers>=4.36
tokenizers>=0.15

# ── Protein Structure Encoder (Step 08: ESM-IF1 pretrained backend) ──
fair-esm>=2.0.0
biotite>=1.0

# ── Data & Visualization ──
matplotlib>=3.8
seaborn>=0.13
openpyxl>=3.1
requests>=2.31
tqdm>=4.66
pyyaml>=6.0
certifi

# ── Benchmarking / ML Baselines ──
xgboost>=2.0

# ── Cross-Dataset (CTRPv2 PharmacoSet extraction — optional) ──
pyreadr>=0.5          # Read R data files; CTRPv2 S4 objects still need R+PharmacoGx

# ── Development ──
pytest>=7.0
pytest-cov>=4.0

# ── Structural Embeddings (PyG GearNet backend for Step 08) ──
# Requires matching torch version. Install separately:
#   pip install torch-geometric -f https://data.pyg.org/whl/torch-2.12.0+cpu.html
